package music;import java.util.ArrayList;/** * The Functional Music project * @author Western State College, CIS412 class */public class MusAfter extends Music {    public Music before;    public Music after;    public MusAfter() {    }    public MusAfter(Music before) {        this.before = before;    }    public MusAfter(Music before, Music after) {        this.before = before;        this.after = after;    }    public BigRational perform(BigRational time, Modifier modifier, ArrayList<MusNote> notes) {                //Perform after, after the before by getting the finish time of the before.        BigRational etime  = this.before.perform(time, modifier, notes);        System.out.println("after perform "+etime);        BigRational fintime = this.after.perform(etime, modifier, notes);        System.out.println("after perform2 "+fintime);        return fintime;    }    public String toString() {        return "MusAfter(" + this.hashCode() + "): " + before.hashCode() + " & " + after.hashCode();    }    public String prettyPrint() {        return "MusAfter(" + this.hashCode() + "): " + before.prettyPrint() + " & " + after.prettyPrint();    }    @Override    public Music reverse() {        return new MusAfter(this.after,this.before);    }    @Override    public BigRational duration() {        return this.after.duration().plus(this.before.duration());    }}